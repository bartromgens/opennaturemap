# Local settings override
# Copy this file to settings_local.py and customize for your local environment
# settings_local.py is gitignored and will not be committed

SECRET_KEY = 'your-secret-key-here'

DEBUG = True

ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# Database configuration (if you want to override the default SQLite)
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / 'db.sqlite3',
#     }
# }
